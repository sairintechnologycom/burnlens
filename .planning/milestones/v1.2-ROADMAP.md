# Milestone v1.2: Account Security & Notifications

**Status:** ✅ SHIPPED 2026-05-06
**Phases:** 11–14
**Total Plans:** 22

## Overview

Close the auth-UX gaps and add server-side alerting so cloud users can recover accounts, verify email ownership, and get notified when spend crosses thresholds — without needing the local proxy running. Also ships budget-aware model downgrade routing in the OSS proxy: silently routes to a cheaper model when budget runs low, zero code change for the user.

## Phases

### Phase 11: Auth Essentials

**Goal**: Cloud users can recover locked accounts and verify email ownership; transactional email infrastructure supports all current and future notification types
**Depends on**: Phase 10 (v1.1 complete)
**Requirements**: AUTH-01–07, EMAIL-01–04 (11 requirements)
**Plans**: 9 plans (01, 02, 03, 03a, 03b, 04, 05, 05a, 05b)

Plans:
- [x] 11-PLAN-01 — `auth_tokens` table + `email_verified_at` column migration
- [x] 11-PLAN-02 — `email.py` TemplateSpec registry + 6 SendGrid templates
- [x] 11-PLAN-03 / 03a — `models.py` + `rate_limit.py` + JWT `email_verified` field
- [x] 11-PLAN-03b — 4 auth route handlers (reset-password, verify-email, resend-verification, confirm-reset)
- [x] 11-PLAN-04 — `billing.py` transaction.completed → send_payment_receipt_email
- [x] 11-PLAN-05 / 05a — `useAuth.ts` emailVerified + forgot-password flow + `BillingStatusBanner`
- [x] 11-PLAN-05b — `/reset-password` page + `/verify-email` page

**Completed:** 2026-05-02

### Phase 12: Cloud Alert Engine

**Goal**: Org owners are automatically notified by email (and optionally Slack) when workspace spend crosses configured budget thresholds — no proxy needed, runs server-side on Railway
**Depends on**: Phase 11
**Requirements**: ALERT-01–07 (7 requirements)
**Plans**: 3 plans

Plans:
- [x] 12-01-PLAN.md — Schema: `alert_rules` + `alert_events` tables + default seeding (80%/100% per plan allowance)
- [x] 12-02-PLAN.md — Alert engine: `burnlens_cloud/alert_engine.py` + 24h dedup + email + Slack dispatch
- [x] 12-03-PLAN.md — Cron endpoint + Slack webhook settings + config + test suite

**Completed:** 2026-05-02

### Phase 13: Alert Management UI

**Goal**: Org owners can view, enable/disable, and edit their workspace alert rules from the cloud dashboard without needing API access
**Depends on**: Phase 12
**Requirements**: ALERT-08–09 (2 requirements)
**Plans**: 3 plans

Plans:
- [x] 13-00-PLAN.md — Wave 0: failing test scaffold for alerts_api.py (8 tests, RED state)
- [x] 13-01-PLAN.md — Backend: `alerts_api.py` GET+PATCH + main.py router registration
- [x] 13-02-PLAN.md — Frontend: `/alerts` page replacement + Sidebar nav entry

**Completed:** 2026-05-06

### Phase 14: Budget-Aware Model Downgrade Routing

**Goal**: When a caller's remaining budget drops below a configurable threshold, BurnLens silently routes to a cheaper model tier instead of blocking with 429 — zero code change for the user
**Depends on**: Phase 13
**Requirements**: ROUTE-01–07 (7 requirements)
**Plans**: 7 plans
**Stream**: OSS proxy (`burnlens/`)

Plans:
- [x] 14-01-PLAN.md — `downgrade.py` provider map + `RoutingConfig` (config.py)
- [x] 14-02-PLAN.md — `RequestRecord` extension + DB migration + `insert_request` update
- [x] 14-03-PLAN.md — `burnlens/proxy/router.py`: `RouteDecision` + `decide_route()`
- [x] 14-04-PLAN.md — `handle_request()` integration + `server.py` config pass-through
- [x] 14-05-PLAN.md — Dashboard `/api/routing-stats` endpoint + Downgrades Today UI
- [x] 14-06-PLAN.md — CLI `burnlens routing` command (`--today`, `--json`)
- [x] 14-07-PLAN.md — `tests/test_router.py` (12 test cases)

**Completed:** 2026-05-05

---

## Milestone Summary

**Stats:**
- 4 phases (11–14), 22 plans, ~150 commits
- 181 files changed, +23,628 / -1,830 lines
- Timeline: 7 days (2026-04-30 → 2026-05-06)

**Key Decisions:**

- `secrets.compare_digest` for constant-time cron secret comparison (timing-safe)
- Cron endpoint fail-open: exceptions from `evaluate_all_workspaces` return `{evaluated:0, fired:0}`
- SSRF guard on Slack webhook: rejects any URL not starting with `https://hooks.slack.com/`
- Verify-email endpoint changed from GET → POST (CR-03 fix) to prevent token leakage in server logs/referrer headers
- `decide_route()` never raises — any exception returns fail-open `RouteDecision(reason="error")`
- Budget priority: customer > team > global_usd > budget_limit_usd (per D-03)
- Team spend cached 60s per team to avoid per-request DB reads
- Deferred imports inside `_resolve_budget()` to break circular router ↔ database dependency
- Body rewrite is JSON-decode + field replace + re-encode; Google URL-path routing is a known limitation

**Issues Resolved:**

- B-01: `WorkspaceBase.owner_email` typed non-optional `str`, received `None` for pre-v1.2 workspaces → 500 on login for grandfathered users; fixed by making field Optional
- CR-01/CR-02: TOCTOU token reuse window on password-reset and verify-email endpoints — fixed with RETURNING clause (mark used_at atomically)
- CR-03: Token in GET query parameter leaked to server logs/referrer — changed to POST body

**Issues Deferred:**

- W-01: Resend-verification button sends empty email for API-key-login users (localStorage `owner_email` never set) — banner shows correctly; resend is silently no-op for this edge case
- Hard ingest 429 quota enforcement — deferred to v1.3 pending real usage data
- Google URL-path model routing (downgrade map uses body rewrite only) — known limitation

**Technical Debt Incurred:**

- 11-05b SUMMARY documents verify-email as GET but code is POST (stale doc, code is correct)
- Phase 11 VERIFICATION.md written pre-fix (07:40 timestamp vs fixes at 12:30) — code confirmed correct in post-fix spot-checks but formal re-verify skipped

---

_For current project status, see .planning/ROADMAP.md_
