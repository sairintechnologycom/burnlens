---
phase: 11-auth-essentials
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - burnlens_cloud/email.py
  - burnlens_cloud/emails/templates/welcome.html
  - burnlens_cloud/emails/templates/verify_email.html
  - burnlens_cloud/emails/templates/password_changed.html
  - burnlens_cloud/emails/templates/payment_receipt.html
  - burnlens_cloud/emails/templates/reset_password.html
autonomous: true
requirements: [EMAIL-01, EMAIL-02, EMAIL-03, EMAIL-04, AUTH-01]
must_haves:
  truths:
    - "TemplateSpec TypedDict with fields: subject (str), template_file (str), required_vars (list[str])"
    - "TEMPLATE_REGISTRY dict maps 'welcome', 'verify_email', 'password_changed', 'payment_receipt' to TemplateSpec entries"
    - "send_welcome_email(recipient_email, workspace_name) — background task, fail-open"
    - "send_verify_email(recipient_email, verify_url) — background task, fail-open"
    - "send_password_changed_email(recipient_email) — background task, fail-open"
    - "send_payment_receipt_email(recipient_email, workspace_name, amount_str, plan_name) — background task, fail-open"
    - "All 4 send functions use track_email_task(asyncio.create_task(_send_background())) pattern"
    - "All 4 HTML template files exist in burnlens_cloud/emails/templates/"
    - "Templates use {{placeholder}} syntax for variable substitution via str.replace()"
  artifacts:
    - path: "burnlens_cloud/email.py"
      provides: "TemplateSpec registry + 4 send_*_email() functions for Phase 11 transactional emails"
      exports: ["TemplateSpec", "TEMPLATE_REGISTRY", "send_welcome_email", "send_verify_email", "send_password_changed_email", "send_payment_receipt_email"]
    - path: "burnlens_cloud/emails/templates/welcome.html"
      provides: "Welcome email HTML template"
      exports: []
    - path: "burnlens_cloud/emails/templates/verify_email.html"
      provides: "Email verification HTML template"
      exports: []
    - path: "burnlens_cloud/emails/templates/password_changed.html"
      provides: "Password changed confirmation HTML template"
      exports: []
    - path: "burnlens_cloud/emails/templates/payment_receipt.html"
      provides: "Payment receipt HTML template"
      exports: []
  key_links:
    - from: "burnlens_cloud/email.py::send_welcome_email"
      to: "Plan 03 signup() — wire welcome email after user creation"
      via: "called after ensure_workspace_member()"
      pattern: "send_welcome_email"
    - from: "burnlens_cloud/email.py::send_verify_email"
      to: "Plan 03 signup() and POST /auth/resend-verification"
      via: "called with verify_url built from token"
      pattern: "send_verify_email"
    - from: "burnlens_cloud/email.py::send_password_changed_email"
      to: "Plan 03 POST /auth/reset-password/confirm"
      via: "called after password update succeeds"
      pattern: "send_password_changed_email"
    - from: "burnlens_cloud/email.py::send_payment_receipt_email"
      to: "Plan 04 _handle_transaction_completed()"
      via: "called from billing.py webhook handler"
      pattern: "send_payment_receipt_email"
---

<objective>
Extend `burnlens_cloud/email.py` with a typed template registry and four new send functions for Phase 11 transactional emails. Also create the four HTML template files they reference.

The registry (`TEMPLATE_REGISTRY`) maps a string key to a `TemplateSpec` so adding a new template in the future is a single-line dict entry. All send functions follow the exact background-task executor pattern from the existing `send_usage_warning_email`: fail-open, `track_email_task()`, `asyncio.create_task()`, SendGrid SDK, `_TEMPLATE_DIR` path resolution.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-auth-essentials/11-CONTEXT.md
@burnlens_cloud/email.py
@burnlens_cloud/emails/templates/usage_80_percent.html

<interfaces>
<!-- Existing send_usage_warning_email pattern (lines 152–289 of email.py):
  - Checks settings.sendgrid_api_key; logs warning and returns if missing
  - Defines inner async def _send_background()
  - Reads template via (_TEMPLATE_DIR / "filename.html").read_text(encoding="utf-8")
  - Does str substitution on template variables
  - Builds Mail object: from_email=Email(settings.sendgrid_from_email), to=[To(email)], subject, html_content=Content("text/html", body)
  - Calls sg = SendGridAPIClient(settings.sendgrid_api_key); sg.send(message)
  - Wraps in try/except Exception — logs on failure, never raises
  - Registers via: track_email_task(asyncio.create_task(_send_background()))
-->
<!-- _TEMPLATE_DIR = Path(__file__).parent / "emails" / "templates"  (line 15 of email.py) -->
<!-- settings.sendgrid_from_email — existing setting used in send_invitation_email -->
<!-- settings.burnlens_frontend_url — existing setting used in send_invitation_email -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TemplateSpec TypedDict and TEMPLATE_REGISTRY to email.py</name>
  <files>burnlens_cloud/email.py</files>
  <read_first>
    - burnlens_cloud/email.py lines 1-20 (imports and _TEMPLATE_DIR declaration)
    - burnlens_cloud/email.py lines 152-175 (send_usage_warning_email — fail-open pattern to replicate)
  </read_first>
  <action>
After the existing imports block (after `from .pii_crypto import decrypt_pii` on line 11), add the TypedDict and registry. Import `TypedDict` from `typing` (add to existing `from typing import Optional` line — change to `from typing import Optional, TypedDict`).

Add immediately after the imports block:

```python
class TemplateSpec(TypedDict):
    subject: str
    template_file: str
    required_vars: list[str]


TEMPLATE_REGISTRY: dict[str, TemplateSpec] = {
    "welcome": {
        "subject": "Welcome to BurnLens",
        "template_file": "welcome.html",
        "required_vars": ["workspace_name"],
    },
    "verify_email": {
        "subject": "Verify your BurnLens email address",
        "template_file": "verify_email.html",
        "required_vars": ["verify_url"],
    },
    "password_changed": {
        "subject": "Your BurnLens password has been changed",
        "template_file": "password_changed.html",
        "required_vars": [],
    },
    "reset_password": {
        "subject": "Reset your BurnLens password",
        "template_file": "reset_password.html",
        "required_vars": ["reset_url"],
    },
    "payment_receipt": {
        "subject": "BurnLens payment receipt",
        "template_file": "payment_receipt.html",
        "required_vars": ["workspace_name", "amount_str", "plan_name"],
    },
}
```
  </action>
  <acceptance_criteria>
    - burnlens_cloud/email.py contains `class TemplateSpec(TypedDict):`
    - burnlens_cloud/email.py contains `TEMPLATE_REGISTRY: dict[str, TemplateSpec] = {`
    - burnlens_cloud/email.py contains keys `"welcome"`, `"verify_email"`, `"password_changed"`, `"payment_receipt"` in TEMPLATE_REGISTRY
    - `from typing import Optional, TypedDict` present (or TypedDict imported via any valid import)
    - Python syntax: `python -c "import ast; ast.parse(open('burnlens_cloud/email.py').read()); print('OK')"` outputs `OK`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add 4 send_*_email() functions to email.py</name>
  <files>burnlens_cloud/email.py</files>
  <read_first>
    - burnlens_cloud/email.py lines 152-298 (send_usage_warning_email — exact pattern to replicate including Mail/Email/To/Content imports already in scope)
    - burnlens_cloud/email.py lines 1-20 (confirm sendgrid imports already present: SendGridAPIClient, Mail, Email, To, Content)
  </read_first>
  <action>
Append the following 4 functions at the END of `burnlens_cloud/email.py` (after line 298):

```python

async def send_welcome_email(recipient_email: str, workspace_name: str) -> None:
    """Send welcome email to new user. Fail-open — never raises."""
    if not settings.sendgrid_api_key:
        logger.warning("send_welcome_email: SendGrid not configured, skipping")
        return

    async def _send_background() -> None:
        try:
            spec = TEMPLATE_REGISTRY["welcome"]
            template = (_TEMPLATE_DIR / spec["template_file"]).read_text(encoding="utf-8")
            html_body = template.replace("{{workspace_name}}", workspace_name)
            message = Mail(
                from_email=Email(settings.sendgrid_from_email),
                to_emails=[To(recipient_email)],
                subject=spec["subject"],
                html_content=Content("text/html", html_body),
            )
            sg = SendGridAPIClient(settings.sendgrid_api_key)
            sg.send(message)
        except Exception:
            logger.exception("send_welcome_email: failed for %s", recipient_email)

    track_email_task(asyncio.create_task(_send_background()))


async def send_verify_email(recipient_email: str, verify_url: str) -> None:
    """Send email-verification link. Fail-open — never raises."""
    if not settings.sendgrid_api_key:
        logger.warning("send_verify_email: SendGrid not configured, skipping")
        return

    async def _send_background() -> None:
        try:
            spec = TEMPLATE_REGISTRY["verify_email"]
            template = (_TEMPLATE_DIR / spec["template_file"]).read_text(encoding="utf-8")
            html_body = template.replace("{{verify_url}}", verify_url)
            message = Mail(
                from_email=Email(settings.sendgrid_from_email),
                to_emails=[To(recipient_email)],
                subject=spec["subject"],
                html_content=Content("text/html", html_body),
            )
            sg = SendGridAPIClient(settings.sendgrid_api_key)
            sg.send(message)
        except Exception:
            logger.exception("send_verify_email: failed for %s", recipient_email)

    track_email_task(asyncio.create_task(_send_background()))


async def send_password_changed_email(recipient_email: str) -> None:
    """Notify user their password was changed. Fail-open — never raises."""
    if not settings.sendgrid_api_key:
        logger.warning("send_password_changed_email: SendGrid not configured, skipping")
        return

    async def _send_background() -> None:
        try:
            spec = TEMPLATE_REGISTRY["password_changed"]
            template = (_TEMPLATE_DIR / spec["template_file"]).read_text(encoding="utf-8")
            message = Mail(
                from_email=Email(settings.sendgrid_from_email),
                to_emails=[To(recipient_email)],
                subject=spec["subject"],
                html_content=Content("text/html", template),
            )
            sg = SendGridAPIClient(settings.sendgrid_api_key)
            sg.send(message)
        except Exception:
            logger.exception("send_password_changed_email: failed for %s", recipient_email)

    track_email_task(asyncio.create_task(_send_background()))


async def send_reset_password_email(recipient_email: str, reset_url: str) -> None:
    """Send password-reset link email. Fail-open — never raises."""
    if not settings.sendgrid_api_key:
        logger.warning("send_reset_password_email: SendGrid not configured, skipping")
        return

    async def _send_background() -> None:
        try:
            spec = TEMPLATE_REGISTRY["reset_password"]
            template = (_TEMPLATE_DIR / spec["template_file"]).read_text(encoding="utf-8")
            html_body = template.replace("{{reset_url}}", reset_url)
            message = Mail(
                from_email=Email(settings.sendgrid_from_email),
                to_emails=[To(recipient_email)],
                subject=spec["subject"],
                html_content=Content("text/html", html_body),
            )
            sg = SendGridAPIClient(settings.sendgrid_api_key)
            sg.send(message)
        except Exception:
            logger.exception("send_reset_password_email: failed for %s", recipient_email)

    track_email_task(asyncio.create_task(_send_background()))


async def send_payment_receipt_email(
    recipient_email: str,
    workspace_name: str,
    amount_str: str,
    plan_name: str,
) -> None:
    """Send payment receipt after successful Paddle transaction. Fail-open — never raises."""
    if not settings.sendgrid_api_key:
        logger.warning("send_payment_receipt_email: SendGrid not configured, skipping")
        return

    async def _send_background() -> None:
        try:
            spec = TEMPLATE_REGISTRY["payment_receipt"]
            template = (_TEMPLATE_DIR / spec["template_file"]).read_text(encoding="utf-8")
            html_body = (
                template
                .replace("{{workspace_name}}", workspace_name)
                .replace("{{amount_str}}", amount_str)
                .replace("{{plan_name}}", plan_name)
            )
            message = Mail(
                from_email=Email(settings.sendgrid_from_email),
                to_emails=[To(recipient_email)],
                subject=spec["subject"],
                html_content=Content("text/html", html_body),
            )
            sg = SendGridAPIClient(settings.sendgrid_api_key)
            sg.send(message)
        except Exception:
            logger.exception("send_payment_receipt_email: failed for %s", recipient_email)

    track_email_task(asyncio.create_task(_send_background()))
```
  </action>
  <acceptance_criteria>
    - burnlens_cloud/email.py contains `async def send_welcome_email(`
    - burnlens_cloud/email.py contains `async def send_verify_email(`
    - burnlens_cloud/email.py contains `async def send_password_changed_email(`
    - burnlens_cloud/email.py contains `async def send_payment_receipt_email(`
    - All 4 functions contain `track_email_task(asyncio.create_task(_send_background()))`
    - All 4 functions check `if not settings.sendgrid_api_key:` and log warning before returning
    - Python syntax: `python -c "import ast; ast.parse(open('burnlens_cloud/email.py').read()); print('OK')"` outputs `OK`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create 4 HTML email template files</name>
  <files>
    burnlens_cloud/emails/templates/welcome.html
    burnlens_cloud/emails/templates/verify_email.html
    burnlens_cloud/emails/templates/password_changed.html
    burnlens_cloud/emails/templates/payment_receipt.html
  </files>
  <read_first>
    - burnlens_cloud/emails/templates/usage_80_percent.html (structural reference — inline CSS, max-width 600px container, BurnLens branding)
  </read_first>
  <action>
Create each file following the `usage_80_percent.html` structure: plain HTML with inline CSS, max-width 600px centered container, `font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif`, button link style `background-color: #3b82f6; color: white; padding: 12px 24px`.

**welcome.html** — variables: `{{workspace_name}}`
```html
<!DOCTYPE html>
<html>
<head><meta charset="utf-8"></head>
<body style="margin:0;padding:0;background:#f9fafb;font-family:-apple-system,BlinkMacSystemFont,'Segoe UI',sans-serif;color:#111827;">
<div style="max-width:600px;margin:40px auto;background:#ffffff;border:1px solid #e5e7eb;border-radius:8px;padding:40px;">
  <h1 style="font-size:20px;font-weight:600;margin:0 0 16px;">Welcome to BurnLens</h1>
  <p style="color:#374151;line-height:1.6;margin:0 0 16px;">Your workspace <strong>{{workspace_name}}</strong> is ready. BurnLens is now intercepting your LLM API calls and tracking costs locally.</p>
  <p style="color:#374151;line-height:1.6;margin:0 0 24px;">Get started: set <code style="background:#f3f4f6;padding:2px 6px;border-radius:3px;font-size:13px;">OPENAI_BASE_URL=http://localhost:8420/proxy/openai</code> in your environment and run your first request.</p>
  <p style="color:#6b7280;font-size:13px;margin:0;">— The BurnLens team</p>
</div>
</body>
</html>
```

**verify_email.html** — variables: `{{verify_url}}`
```html
<!DOCTYPE html>
<html>
<head><meta charset="utf-8"></head>
<body style="margin:0;padding:0;background:#f9fafb;font-family:-apple-system,BlinkMacSystemFont,'Segoe UI',sans-serif;color:#111827;">
<div style="max-width:600px;margin:40px auto;background:#ffffff;border:1px solid #e5e7eb;border-radius:8px;padding:40px;">
  <h1 style="font-size:20px;font-weight:600;margin:0 0 16px;">Verify your email address</h1>
  <p style="color:#374151;line-height:1.6;margin:0 0 24px;">Click the button below to verify your email address. This link expires in 24 hours and can only be used once.</p>
  <a href="{{verify_url}}" style="display:inline-block;background:#3b82f6;color:#ffffff;padding:12px 24px;text-decoration:none;border-radius:4px;font-weight:500;font-size:14px;">Verify email address</a>
  <p style="color:#6b7280;font-size:13px;margin:24px 0 0;">Or copy this link: <code style="background:#f3f4f6;padding:2px 4px;border-radius:2px;font-size:12px;">{{verify_url}}</code></p>
  <p style="color:#6b7280;font-size:13px;margin:8px 0 0;">If you didn't create a BurnLens account, you can safely ignore this email.</p>
</div>
</body>
</html>
```

**password_changed.html** — no variables
```html
<!DOCTYPE html>
<html>
<head><meta charset="utf-8"></head>
<body style="margin:0;padding:0;background:#f9fafb;font-family:-apple-system,BlinkMacSystemFont,'Segoe UI',sans-serif;color:#111827;">
<div style="max-width:600px;margin:40px auto;background:#ffffff;border:1px solid #e5e7eb;border-radius:8px;padding:40px;">
  <h1 style="font-size:20px;font-weight:600;margin:0 0 16px;">Your password has been changed</h1>
  <p style="color:#374151;line-height:1.6;margin:0 0 16px;">Your BurnLens account password was successfully changed.</p>
  <p style="color:#374151;line-height:1.6;margin:0 0 24px;">If you did not make this change, please contact support immediately and consider resetting your password again.</p>
  <p style="color:#6b7280;font-size:13px;margin:0;">— The BurnLens team</p>
</div>
</body>
</html>
```

**reset_password.html** — variables: `{{reset_url}}`
```html
<!DOCTYPE html>
<html>
<head><meta charset="utf-8"></head>
<body style="margin:0;padding:0;background:#f9fafb;font-family:-apple-system,BlinkMacSystemFont,'Segoe UI',sans-serif;color:#111827;">
<div style="max-width:600px;margin:40px auto;background:#ffffff;border:1px solid #e5e7eb;border-radius:8px;padding:40px;">
  <h1 style="font-size:20px;font-weight:600;margin:0 0 16px;">Reset your BurnLens password</h1>
  <p style="color:#374151;line-height:1.6;margin:0 0 24px;">Click the button below to set a new password. This link expires in 1 hour and can only be used once.</p>
  <a href="{{reset_url}}" style="display:inline-block;background:#3b82f6;color:#ffffff;padding:12px 24px;text-decoration:none;border-radius:4px;font-weight:500;font-size:14px;">Reset password</a>
  <p style="color:#6b7280;font-size:13px;margin:24px 0 0;">Or copy this link: <code style="background:#f3f4f6;padding:2px 4px;border-radius:2px;font-size:12px;">{{reset_url}}</code></p>
  <p style="color:#6b7280;font-size:13px;margin:8px 0 0;">If you didn't request a reset, ignore this email — your password is unchanged.</p>
</div>
</body>
</html>
```

**payment_receipt.html** — variables: `{{workspace_name}}`, `{{amount_str}}`, `{{plan_name}}`
```html
<!DOCTYPE html>
<html>
<head><meta charset="utf-8"></head>
<body style="margin:0;padding:0;background:#f9fafb;font-family:-apple-system,BlinkMacSystemFont,'Segoe UI',sans-serif;color:#111827;">
<div style="max-width:600px;margin:40px auto;background:#ffffff;border:1px solid #e5e7eb;border-radius:8px;padding:40px;">
  <h1 style="font-size:20px;font-weight:600;margin:0 0 16px;">Payment receipt</h1>
  <p style="color:#374151;line-height:1.6;margin:0 0 16px;">Thank you for your payment. Here is your receipt for the <strong>{{plan_name}}</strong> plan.</p>
  <table style="width:100%;border-collapse:collapse;margin:0 0 24px;">
    <tr style="border-bottom:1px solid #e5e7eb;">
      <td style="padding:12px 0;color:#374151;">Workspace</td>
      <td style="padding:12px 0;text-align:right;font-weight:500;">{{workspace_name}}</td>
    </tr>
    <tr style="border-bottom:1px solid #e5e7eb;">
      <td style="padding:12px 0;color:#374151;">Plan</td>
      <td style="padding:12px 0;text-align:right;font-weight:500;">{{plan_name}}</td>
    </tr>
    <tr>
      <td style="padding:12px 0;color:#374151;font-weight:600;">Amount charged</td>
      <td style="padding:12px 0;text-align:right;font-weight:600;">{{amount_str}}</td>
    </tr>
  </table>
  <p style="color:#6b7280;font-size:13px;margin:0;">Questions? Reply to this email or visit burnlens.app.</p>
</div>
</body>
</html>
```
  </action>
  <acceptance_criteria>
    - File `burnlens_cloud/emails/templates/welcome.html` exists and contains `{{workspace_name}}`
    - File `burnlens_cloud/emails/templates/verify_email.html` exists and contains `{{verify_url}}`
    - File `burnlens_cloud/emails/templates/password_changed.html` exists and contains `Your BurnLens account password was successfully changed`
    - File `burnlens_cloud/emails/templates/payment_receipt.html` exists and contains `{{workspace_name}}` and `{{amount_str}}` and `{{plan_name}}`
    - All 4 files contain `<!DOCTYPE html>`
    - `ls burnlens_cloud/emails/templates/` shows all 4 new files alongside existing ones
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `grep -c "class TemplateSpec" burnlens_cloud/email.py` → `1`
2. `grep -c "TEMPLATE_REGISTRY" burnlens_cloud/email.py` → at least `2` (definition + usage)
3. `grep -l "send_welcome_email\|send_verify_email\|send_password_changed_email\|send_payment_receipt_email" burnlens_cloud/email.py` → shows file
4. `ls burnlens_cloud/emails/templates/welcome.html burnlens_cloud/emails/templates/verify_email.html burnlens_cloud/emails/templates/password_changed.html burnlens_cloud/emails/templates/payment_receipt.html` → all 4 exist (exit 0)
5. `python -c "import ast; ast.parse(open('burnlens_cloud/email.py').read()); print('OK')"` → `OK`
</verification>

<threat_model>
## Security Threat Model (ASVS L1)

| Threat | Severity | Mitigation |
|--------|----------|-----------|
| Template injection via user-controlled variables | HIGH | Variables substituted via `str.replace("{{var}}", value)` only — no eval, no f-strings with untrusted input; values come from DB (workspace_name) or backend-generated URLs (verify_url, amount_str) |
| Email header injection (recipient/subject) | HIGH | recipient_email comes from DB (encrypted + validated at signup); subject is a literal string constant from TemplateSpec — never contains user input |
| SendGrid API key leak in logs | MEDIUM | `settings.sendgrid_api_key` is never logged; only `recipient_email` appears in exception log lines |
| Fail-open silently drops verification emails | LOW | Accepted trade-off per CONTEXT.md D-03; warning logged when SendGrid key missing; Plan 03 resend endpoint provides recovery path |
| XSS in HTML templates | LOW | Templates contain only static HTML + safe substituted values; `workspace_name` from DB is not user-controlled in a way that allows script injection in email clients |

No high-severity unmitigated threats.
</threat_model>

<must_haves>
- TemplateSpec TypedDict and TEMPLATE_REGISTRY dict present in email.py
- 4 send_*_email() functions appended to email.py — all fail-open and background-task based
- All 4 HTML template files created with correct {{placeholder}} variables
- No changes to existing send_invitation_email or send_usage_warning_email functions
- email.py parses without syntax errors
</must_haves>
