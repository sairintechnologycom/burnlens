---
context: default
phase: post-v1.2-cleanup
task: 1
total_tasks: 2
status: in_progress
last_updated: 2026-05-04T13:20:00Z
---

# No Blocking Constraints

_No failures or anti-patterns were discovered this session._

<current_state>
PyPI v1.2.0 is live. Main branch is clean at aaaad18. One quick fix remains
before returning to v1.2 milestone work: bump GitHub Actions from Node 20 to
Node 24 before the June 2 forced deadline.
</current_state>

<completed_work>

- Task 1 (DONE, commit aaaad18): Fix test_settings_api.py + test_custom_pricing.py
  — 22/22 tests now passing. Three root causes fixed:
  1. Added `cloud_client` fixture to conftest.py yielding `(AsyncClient, FastAPI app)` — previously both files pointed at the OSS proxy app instead of the cloud settings router.
  2. Switched from `patch("...verify_token")` to `app.dependency_overrides[verify_token]` — FastAPI Depends holds direct function references; module patches are silently ignored.
  3. Added `await` to all 6 `require_role()` call sites in `burnlens_cloud/settings_api.py` — missing await meant role checks never fired.
  4. Fixed `_make_db_mock()` — asyncpg `transaction()` needs `MagicMock` (not `AsyncMock`) so it returns a context manager, not a coroutine.
  5. Added `get_encryption_manager` mock to OTEL success test.
</completed_work>

<remaining_work>

- Task 2 (not started): Bump `actions/checkout@v4` → `@v5` in `.github/workflows/publish.yml`
  Node 20 in GitHub Actions is deprecated, forced to Node 24 on June 2 2026.
  File: `.github/workflows/publish.yml`
  Check: `actions/setup-python@v5` may already be Node 24 compatible — verify before changing.

- After task 2: Check Phase 12 (Cloud Alert Engine) completion status. Directory has
  3 plans + 3 summaries. If complete, move to Phase 13 (Alert Management UI) or
  close v1.2 milestone.
</remaining_work>

<decisions_made>

- `app.dependency_overrides` is the only correct way to mock FastAPI `Depends()` in tests
- `MagicMock` (not `AsyncMock`) for `transaction()` — asyncpg connection mock pattern
- Production bug fixed: `require_role()` must be `await`ed in settings_api.py
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading
1. `.planning/HANDOFF.json` — structured state with full decision log

## Infrastructure State
- PyPI: `burnlens==1.2.0` is live
- Azure DevOps remote: `main` is at aaaad18
- `.planning/STATE.md` has an uncommitted cosmetic update (last_activity date) — can be committed with the GH Actions fix or separately

<context>
Clean handoff. One quick fix (GH Actions bump) before resuming v1.2 phase work.
</context>

<next_action>
Start with: open `.github/workflows/publish.yml`, change `actions/checkout@v4` to
`actions/checkout@v5`, commit, push. Then check phase 12 completion and route to
phase 13 or milestone close.
</next_action>
