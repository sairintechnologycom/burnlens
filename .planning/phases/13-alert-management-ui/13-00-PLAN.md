---
phase: 13-alert-management-ui
plan: "00"
type: tdd
wave: 0
depends_on: []
files_modified:
  - tests/test_phase13_alerts_api.py
autonomous: true
requirements:
  - ALERT-08
  - ALERT-09

must_haves:
  truths:
    - "test_phase13_alerts_api.py exists with 8 failing test stubs covering all ALERT-08 and ALERT-09 cases"
    - "All 8 tests are importable (no syntax errors) but fail because alerts_api.py does not yet exist"
  artifacts:
    - path: "tests/test_phase13_alerts_api.py"
      provides: "8 pytest test stubs for GET and PATCH alert-rules endpoints"
      contains: "_make_alerts_app"
  key_links:
    - from: "tests/test_phase13_alerts_api.py"
      to: "burnlens_cloud.alerts_api"
      via: "_make_alerts_app() import"
      pattern: "from burnlens_cloud.alerts_api import router"
---

<objective>
Create the failing test scaffold for the alerts API. Wave 0 establishes 8 test stubs that fully specify the expected behaviour of GET /api/v1/alert-rules and PATCH /api/v1/alert-rules/{rule_id}. All tests must be importable but fail with ImportError or collection error because alerts_api.py does not yet exist.

Purpose: Establish RED state before Wave 1 writes the implementation. Prevents Wave 1 executor from making false-green tests by writing tests and code together.
Output: tests/test_phase13_alerts_api.py with 8 test functions, all failing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Write failing test stubs for alerts_api.py (RED state)</name>
  <files>tests/test_phase13_alerts_api.py</files>
  <read_first>
    - tests/test_settings_api.py (exact analog — fixture pattern, _auth helper, AsyncClient/ASGITransport usage, dependency_overrides)
    - tests/test_phase12_alerts.py (_make_cron_app pattern to replicate as _make_alerts_app)
    - tests/conftest.py (shared fixtures available — do not duplicate)
    - burnlens_cloud/models.py (TokenPayload fields — workspace_id, user_id, role, plan, iat, exp)
  </read_first>
  <behavior>
    All 8 tests must be in RED state (fail or error) when run before Plan 01 ships alerts_api.py.

    Tests to write:

    class TestAlertRulesGet:
      - test_list_alert_rules_200: owner_token GET /api/v1/alert-rules with mocked execute_query returning 1 row → 200, list of length 1, threshold_pct == 80
      - test_list_rules_viewer_allowed: viewer_token GET /api/v1/alert-rules → 200 (viewer role is allowed for GET)
      - test_list_rules_scoped_to_workspace: owner_token GET, mock execute_query, assert it was called with token.workspace_id as the first param (workspace isolation)

    class TestAlertRulesPatch:
      - test_patch_toggle_enabled: owner_token PATCH /api/v1/alert-rules/{uuid} body={enabled: true}, mock execute_insert returning "UPDATE 1" → 200
      - test_patch_idor_protection: owner_token PATCH with mock execute_insert returning "UPDATE 0" → 404 with detail "rule_not_found"
      - test_patch_invalid_threshold: owner_token PATCH body={threshold_pct: 50} → 422, execute_insert NOT called
      - test_patch_extra_emails: owner_token PATCH body={extra_emails: ["a@b.com"]} mock execute_insert "UPDATE 1" → 200
      - test_patch_viewer_forbidden: viewer_token PATCH → 403

    Infrastructure:
    - _make_alerts_app() factory: creates a bare FastAPI app with alerts_router mounted
    - _auth(app, token) helper: sets app.dependency_overrides[_verify_token] = lambda: token
    - owner_token fixture: TokenPayload(workspace_id=uuid4(), user_id=uuid4(), role="owner", plan="cloud", iat=now, exp=now+86400)
    - viewer_token fixture: same but role="viewer"
    - All tests decorated @pytest.mark.asyncio
    - All tests use httpx.AsyncClient(transport=ASGITransport(app=app), base_url="http://testserver")
    - Mock target strings: "burnlens_cloud.alerts_api.execute_query", "burnlens_cloud.alerts_api.execute_insert"
  </behavior>
  <action>
Create tests/test_phase13_alerts_api.py with the following exact content structure:

```
"""Tests for alerts API endpoints (GET /api/v1/alert-rules, PATCH /api/v1/alert-rules/{id})."""

import pytest
from unittest.mock import patch, AsyncMock
from uuid import uuid4
import time

from burnlens_cloud.models import TokenPayload
from burnlens_cloud.auth import verify_token as _verify_token


def _auth(app, token):
    """Override the verify_token FastAPI dependency for a single test."""
    app.dependency_overrides[_verify_token] = lambda: token


def _make_alerts_app():
    from fastapi import FastAPI
    from burnlens_cloud.alerts_api import router as alerts_router
    app = FastAPI()
    app.include_router(alerts_router)
    return app


@pytest.fixture
def owner_token():
    return TokenPayload(
        workspace_id=uuid4(), user_id=uuid4(),
        role="owner", plan="cloud",
        iat=int(time.time()), exp=int(time.time()) + 86400,
    )


@pytest.fixture
def viewer_token():
    return TokenPayload(
        workspace_id=uuid4(), user_id=uuid4(),
        role="viewer", plan="cloud",
        iat=int(time.time()), exp=int(time.time()) + 86400,
    )


class TestAlertRulesGet:

    @pytest.mark.asyncio
    async def test_list_alert_rules_200(self, owner_token):
        from httpx import AsyncClient, ASGITransport
        app = _make_alerts_app()
        _auth(app, owner_token)
        mock_rows = [
            {"id": str(uuid4()), "threshold_pct": 80, "channel": "email",
             "enabled": True, "has_slack": False, "extra_emails": [],
             "created_at": "2026-01-01T00:00:00", "updated_at": "2026-01-01T00:00:00"},
        ]
        with patch("burnlens_cloud.alerts_api.execute_query", AsyncMock(return_value=mock_rows)):
            transport = ASGITransport(app=app)
            async with AsyncClient(transport=transport, base_url="http://testserver") as ac:
                response = await ac.get("/api/v1/alert-rules")
        assert response.status_code == 200
        assert len(response.json()) == 1
        assert response.json()[0]["threshold_pct"] == 80

    @pytest.mark.asyncio
    async def test_list_rules_viewer_allowed(self, viewer_token):
        from httpx import AsyncClient, ASGITransport
        app = _make_alerts_app()
        _auth(app, viewer_token)
        with patch("burnlens_cloud.alerts_api.execute_query", AsyncMock(return_value=[])):
            transport = ASGITransport(app=app)
            async with AsyncClient(transport=transport, base_url="http://testserver") as ac:
                response = await ac.get("/api/v1/alert-rules")
        assert response.status_code == 200

    @pytest.mark.asyncio
    async def test_list_rules_scoped_to_workspace(self, owner_token):
        from httpx import AsyncClient, ASGITransport
        app = _make_alerts_app()
        _auth(app, owner_token)
        mock_execute_query = AsyncMock(return_value=[])
        with patch("burnlens_cloud.alerts_api.execute_query", mock_execute_query):
            transport = ASGITransport(app=app)
            async with AsyncClient(transport=transport, base_url="http://testserver") as ac:
                await ac.get("/api/v1/alert-rules")
        # First positional arg after the SQL string must be the token workspace_id
        call_args = mock_execute_query.call_args
        assert call_args is not None
        assert owner_token.workspace_id in call_args.args or owner_token.workspace_id in call_args.kwargs.values()


class TestAlertRulesPatch:

    @pytest.mark.asyncio
    async def test_patch_toggle_enabled(self, owner_token):
        from httpx import AsyncClient, ASGITransport
        app = _make_alerts_app()
        _auth(app, owner_token)
        with patch("burnlens_cloud.alerts_api.execute_insert", AsyncMock(return_value="UPDATE 1")):
            transport = ASGITransport(app=app)
            async with AsyncClient(transport=transport, base_url="http://testserver") as ac:
                response = await ac.patch(
                    f"/api/v1/alert-rules/{uuid4()}",
                    json={"enabled": True},
                )
        assert response.status_code == 200

    @pytest.mark.asyncio
    async def test_patch_idor_protection(self, owner_token):
        """UPDATE with workspace_id mismatch returns 0 rows -> 404, not 200."""
        from httpx import AsyncClient, ASGITransport
        app = _make_alerts_app()
        _auth(app, owner_token)
        with patch("burnlens_cloud.alerts_api.execute_insert", AsyncMock(return_value="UPDATE 0")):
            transport = ASGITransport(app=app)
            async with AsyncClient(transport=transport, base_url="http://testserver") as ac:
                response = await ac.patch(
                    f"/api/v1/alert-rules/{uuid4()}",
                    json={"enabled": True},
                )
        assert response.status_code == 404
        assert response.json()["detail"] == "rule_not_found"

    @pytest.mark.asyncio
    async def test_patch_invalid_threshold(self, owner_token):
        from httpx import AsyncClient, ASGITransport
        app = _make_alerts_app()
        _auth(app, owner_token)
        mock_insert = AsyncMock()
        with patch("burnlens_cloud.alerts_api.execute_insert", mock_insert):
            transport = ASGITransport(app=app)
            async with AsyncClient(transport=transport, base_url="http://testserver") as ac:
                response = await ac.patch(
                    f"/api/v1/alert-rules/{uuid4()}",
                    json={"threshold_pct": 50},
                )
        assert response.status_code == 422
        mock_insert.assert_not_called()

    @pytest.mark.asyncio
    async def test_patch_extra_emails(self, owner_token):
        from httpx import AsyncClient, ASGITransport
        app = _make_alerts_app()
        _auth(app, owner_token)
        with patch("burnlens_cloud.alerts_api.execute_insert", AsyncMock(return_value="UPDATE 1")):
            transport = ASGITransport(app=app)
            async with AsyncClient(transport=transport, base_url="http://testserver") as ac:
                response = await ac.patch(
                    f"/api/v1/alert-rules/{uuid4()}",
                    json={"extra_emails": ["test@example.com"]},
                )
        assert response.status_code == 200

    @pytest.mark.asyncio
    async def test_patch_viewer_forbidden(self, viewer_token):
        from httpx import AsyncClient, ASGITransport
        app = _make_alerts_app()
        _auth(app, viewer_token)
        transport = ASGITransport(app=app)
        async with AsyncClient(transport=transport, base_url="http://testserver") as ac:
            response = await ac.patch(
                f"/api/v1/alert-rules/{uuid4()}",
                json={"enabled": True},
            )
        assert response.status_code == 403
```

After writing, confirm RED state by running the test command and verifying it fails with ImportError or ModuleNotFoundError (alerts_api.py does not exist yet).
  </action>
  <verify>
    <automated>cd /Users/bhushan/Documents/Projects/burnlens && python -m pytest tests/test_phase13_alerts_api.py --collect-only 2>&1 | grep -E "ERROR|error|ImportError|ModuleNotFound" | head -5</automated>
  </verify>
  <done>
    - tests/test_phase13_alerts_api.py exists and is syntactically valid Python
    - Running pytest on it produces ImportError or collection error (RED state — alerts_api.py not yet written)
    - All 8 test function names are present: test_list_alert_rules_200, test_list_rules_viewer_allowed, test_list_rules_scoped_to_workspace, test_patch_toggle_enabled, test_patch_idor_protection, test_patch_invalid_threshold, test_patch_extra_emails, test_patch_viewer_forbidden
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → alerts_api module | Test scaffold imports from burnlens_cloud.alerts_api — no trust boundary, test-only scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-00-01 | Tampering | test_phase13_alerts_api.py | accept | Test file only; no production code path. No mitigation required. |
</threat_model>

<verification>
- pytest tests/test_phase13_alerts_api.py --collect-only fails with ImportError (alerts_api.py absent) — confirms RED state
- All 8 test names present: grep -c "async def test_" tests/test_phase13_alerts_api.py returns 8
- _make_alerts_app function present: grep -c "_make_alerts_app" tests/test_phase13_alerts_api.py returns >= 2
</verification>

<success_criteria>
tests/test_phase13_alerts_api.py exists, is syntactically valid, contains exactly 8 async test methods, and fails at collection time because burnlens_cloud/alerts_api.py does not yet exist.
</success_criteria>

<output>
After completion, create `.planning/phases/13-alert-management-ui/13-00-SUMMARY.md`
</output>
