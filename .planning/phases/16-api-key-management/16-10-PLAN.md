---
phase: 16-api-key-management
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/phases/16-api-key-management/16-VERIFICATION.md
  - frontend/src/app/api-keys/page.tsx
  - frontend/src/components/ApiKeysTable.tsx
autonomous: false
gap_closure: true
requirements:
  - APIKEY-05
user_setup: []

must_haves:
  truths:
    - "The ROADMAP Phase 16 success criterion #5 wording and the implementation (per D-04) are reconciled — exactly one of the two has been adjusted, the other unchanged"
    - "If path (a) chosen: viewer-role sessions cannot see or click the Create-key button, and cannot click Revoke on any row — verified by a Playwright spec asserting role-gated visibility"
    - "If path (b) chosen: ROADMAP SC-5 wording matches D-04 (viewers can self-create and self-revoke their own keys), and 16-VERIFICATION.md has an `overrides_applied` entry recording the deviation"
  artifacts:
    - path: ".planning/ROADMAP.md (path b) OR frontend/src/app/api-keys/page.tsx + ApiKeysTable.tsx (path a)"
      provides: "The single source of truth that no longer contradicts the other"
      contains: "viewer"
    - path: ".planning/phases/16-api-key-management/16-VERIFICATION.md"
      provides: "Frontmatter `overrides_applied` count incremented OR `gaps` SC-5 entry marked resolved with rationale"
      contains: "overrides_applied"
  key_links:
    - from: "ROADMAP SC-5"
      to: "D-04 in CONTEXT.md"
      via: "human decision (a) tighten UI to ROADMAP, OR (b) update ROADMAP to match D-04"
      pattern: "viewer"
---

<objective>
Close the SC-5 ↔ D-04 contract divergence flagged in 16-VERIFICATION.md as UNCERTAIN. The server-side scoping (`_viewer_creator_filter` on GET/PATCH/DELETE) is correct and verified by tests. The UI shows Create/Revoke to viewers, which matches D-04 ("viewers can self-create and self-revoke their own keys") but contradicts the verbatim ROADMAP wording ("cannot access the create or revoke actions").

Either the ROADMAP must change to match the implementation, or the implementation must change to match the ROADMAP. The two cannot coexist. A human decision is required to pick which side moves.

Purpose: Eliminate a documented contract/code disagreement so APIKEY-05 has a single source of truth, and so the next milestone planner does not inherit the ambiguity.
Output: Either (a) UI role-gates added + a Playwright test asserting them, OR (b) ROADMAP SC-5 wording updated + VERIFICATION.md override recorded. Exactly one path is taken — never both.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-api-key-management/16-CONTEXT.md
@.planning/phases/16-api-key-management/16-VERIFICATION.md
@.planning/phases/16-api-key-management/16-PATTERNS.md
@frontend/src/app/api-keys/page.tsx
@frontend/src/components/ApiKeysTable.tsx
@CLAUDE.md

<interfaces>
<!-- The two artifacts in disagreement: -->

ROADMAP.md (.planning/ROADMAP.md, search for "Phase 16: API Key Management" Success Criteria #5):
> 5. A viewer-role user visiting `/api-keys` sees only their own key and cannot access the create or revoke actions

CONTEXT.md (.planning/phases/16-api-key-management/16-CONTEXT.md, D-04 entry):
> "Viewers can self-create and self-revoke their own keys. Cross-tenant or wrong-creator access returns 404 (indistinguishability)."

frontend/src/app/api-keys/page.tsx:164-171 — Create button (no role gate today):
```tsx
<button
  className="btn btn-cyan"
  onClick={() => setShowCreate(true)}
  type="button"
>
  Create key
</button>
```

frontend/src/app/api-keys/page.tsx:197-202 — Table props (`canMutateRow` is constant-true today):
```tsx
<ApiKeysTable
  keys={keys}
  onRequestRevoke={(k) => setPendingRevoke(k)}
  onSaveLabel={handleSaveLabel}
  canMutateRow={() => true /* server enforces role/creator scoping */}
/>
```

<!-- D-04 is the more recent and security-equivalent decision; ROADMAP SC-5 was written before D-04 was locked. The recommended resolution is path (b). -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Decide which side of the SC-5 ↔ D-04 contradiction to move</name>
  <files>n/a (decision-only checkpoint — no files written in this task)</files>
  <action>Pause for human decision. Present the two options below and wait for the human to type `path-a` or `path-b`. Take NO file actions in this task — the chosen path determines whether Task 2 (path-b) or Task 3 (path-a) runs in this plan; the OTHER task must be skipped entirely.</action>
  <decision>How to close the APIKEY-05 contract divergence between ROADMAP SC-5 and CONTEXT D-04.</decision>
  <context>
    Server-side enforcement is correct and shipped. The disagreement is only between two documentation+UI surfaces. The next milestone planner cannot inherit a contradiction — exactly one source must move. The verifier flagged this and explicitly punted to a human decision.

    Security-equivalence note: both options are security-equivalent — the server enforces viewer-creator scoping identically regardless. The only difference is UX (does the viewer SEE a Create button) and which artifact records "truth".

    Recommendation: path (b). D-04 is the more recent locked decision, the implementation already matches it, and revoking working UI to retighten a UX choice is wasteful. But this is your call.
  </context>
  <options>
    <option id="path-a">
      <name>(a) Tighten UI to match ROADMAP SC-5 verbatim</name>
      <pros>
        - ROADMAP success criteria remain authoritative and unedited.
        - One fewer "thing that surprised the user" if a viewer is testing the app expecting the documented behaviour.
      </pros>
      <cons>
        - Requires production code changes: role gate on Create button, role-aware `canMutateRow`, and a Playwright test.
        - Contradicts the most recently locked decision (D-04) — leaves D-04 to be re-revisited later.
        - Re-tightening working code is rework, not value.
      </cons>
    </option>
    <option id="path-b">
      <name>(b) Update ROADMAP SC-5 wording to match D-04 (RECOMMENDED)</name>
      <pros>
        - No production code changes — implementation already matches the new wording.
        - Aligns with the most recent locked decision.
        - Zero risk of regression — purely documentation.
        - Verifier already suggested this resolution.
      </pros>
      <cons>
        - ROADMAP success criteria are amended post-hoc (small precedent concern — recorded as an explicit override).
      </cons>
    </option>
  </options>
  <verify>Human selects exactly one of: path-a, path-b. The selection is recorded in 16-10-SUMMARY.md.</verify>
  <done>One of path-a or path-b is selected and recorded. Execute the corresponding follow-up task (Task 2 for path-b, Task 3 for path-a); skip the other.</done>
  <resume-signal>Select: path-a or path-b</resume-signal>
</task>

<task type="auto">
  <name>Task 2 (IF path-b SELECTED): Update ROADMAP wording and record override</name>
  <files>.planning/ROADMAP.md, .planning/phases/16-api-key-management/16-VERIFICATION.md</files>
  <read_first>
    - .planning/ROADMAP.md (full file — locate the Phase 16 "Success Criteria" block; the file is short)
    - .planning/phases/16-api-key-management/16-VERIFICATION.md (lines 1-9 — to update frontmatter `overrides_applied`)
    - .planning/phases/16-api-key-management/16-CONTEXT.md (D-04 verbatim text — to quote in the override entry)
  </read_first>
  <action>
    **Skip this task entirely if path-a was chosen.** Otherwise:

    1. **Update `.planning/ROADMAP.md` Phase 16 Success Criterion #5.** Find the line that reads (or close to):
       ```
       5. A viewer-role user visiting `/api-keys` sees only their own key and cannot access the create or revoke actions
       ```
       Replace with:
       ```
       5. A viewer-role user visiting `/api-keys` sees only keys they created and cannot access keys created by other users (per D-04 — viewers may self-create and self-revoke their own keys; cross-creator access returns 404 indistinguishability)
       ```

    2. **Update `.planning/phases/16-api-key-management/16-VERIFICATION.md` frontmatter.**
       - Change `overrides_applied: 0` → `overrides_applied: 1`.
       - Add an `overrides:` block immediately below `overrides_applied:`:
         ```yaml
         overrides:
           - rule: "SC-5 verbatim wording 'cannot access the create or revoke actions'"
             accepted_by: "human (16-10 plan, path-b)"
             rationale: "Server-side enforcement of viewer-creator scoping is security-equivalent to a UI gate. D-04 (locked decision) explicitly permits viewers to self-create and self-revoke their own keys. ROADMAP wording predates D-04 and is the artefact that moves. No production code change."
             date: "2026-05-12"
         ```
       - In the `gaps:` list, change the SC-5 entry's `status: partial` to `status: resolved_via_override` and add a one-line `resolution:` field referencing this plan.

    3. Do NOT touch any other file. Do NOT modify production code.
  </action>
  <verify>
    <automated>grep -c "viewers may self-create and self-revoke" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "cannot access the create or revoke actions" .planning/ROADMAP.md` returns 0 (old wording gone)
    - `grep -c "per D-04" .planning/ROADMAP.md` returns ≥ 1 (new wording present, references the decision)
    - `grep -c "overrides_applied: 1" .planning/phases/16-api-key-management/16-VERIFICATION.md` returns 1
    - `grep -c "resolved_via_override" .planning/phases/16-api-key-management/16-VERIFICATION.md` returns ≥ 1
    - `git diff --stat .planning/ROADMAP.md .planning/phases/16-api-key-management/16-VERIFICATION.md` shows changes confined to those two files
    - Zero source code files modified: `git diff --name-only frontend/ burnlens/ burnlens_cloud/ tests/` returns empty
  </acceptance_criteria>
  <done>ROADMAP SC-5 matches D-04. Override recorded. No production code touched. The two-artefact contradiction is resolved.</done>
</task>

<task type="auto">
  <name>Task 3 (IF path-a SELECTED): Add UI role gates + Playwright test</name>
  <files>frontend/src/app/api-keys/page.tsx, frontend/src/components/ApiKeysTable.tsx, frontend/tests/e2e/phase16_viewer_role.spec.ts</files>
  <read_first>
    - frontend/src/app/api-keys/page.tsx (the Create-button site at lines 164-171 and the ApiKeysTable callsite at lines 197-202)
    - frontend/src/components/ApiKeysTable.tsx (full file — to see how `canMutateRow` is consumed)
    - frontend/src/lib/hooks/useAuth.ts (to confirm `session.role` is exposed; if not, gate by whatever role field IS exposed)
    - frontend/tests/e2e/phase11_auth.spec.ts (Playwright mock pattern reference)
    - frontend/AGENTS.md (Next.js deviations from training data — read before editing)
  </read_first>
  <action>
    **Skip this task entirely if path-b was chosen.** Otherwise:

    1. In `frontend/src/app/api-keys/page.tsx`, derive a role gate near the top of the component (just after the existing `session`/`logout` destructure from `useAuth`):
       ```typescript
       const canMutate = session?.role !== "viewer";
       ```

    2. Wrap the Create-key button at lines 164-171 to hide it for viewers:
       ```tsx
       {canMutate && (
         <button
           className="btn btn-cyan"
           onClick={() => setShowCreate(true)}
           type="button"
         >
           Create key
         </button>
       )}
       ```

    3. Update the `ApiKeysTable` callsite at lines 197-202 to pass the real `canMutate` value:
       ```tsx
       <ApiKeysTable
         keys={keys}
         onRequestRevoke={(k) => setPendingRevoke(k)}
         onSaveLabel={handleSaveLabel}
         canMutateRow={() => canMutate}
       />
       ```

    4. **Inspect `ApiKeysTable.tsx`** to confirm `canMutateRow(key)` is already consulted before rendering the Revoke and Edit affordances. If it isn't (e.g. Revoke is rendered unconditionally), wrap the Revoke/Edit JSX in `{canMutateRow(k) && (...)}` blocks. Do not change the prop's signature — keep `canMutateRow: (key: ApiKey) => boolean`.

    5. **Create a Playwright spec** at `frontend/tests/e2e/phase16_viewer_role.spec.ts` that:
       - Seeds localStorage with `session.role = "viewer"` (use the same `addInitScript` pattern as `phase16_resend_banner.spec.ts`).
       - Mocks `GET /api-keys` → returns one row whose `created_by_user_id` matches the viewer.
       - Asserts `page.getByRole('button', { name: /create key/i })` is NOT visible.
       - Asserts no `Revoke` button is reachable on the visible row.
       - Then re-runs with `session.role = "owner"` and asserts both ARE visible (sanity check that the gate is role-driven, not always-hidden).

    6. Run `cd frontend && npx playwright test tests/e2e/phase16_viewer_role.spec.ts --reporter=list` — both viewer + owner assertions must pass.
  </action>
  <verify>
    <automated>grep -c "session?.role !== \"viewer\"" frontend/src/app/api-keys/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "session?.role !== \"viewer\"" frontend/src/app/api-keys/page.tsx` returns 1
    - `grep -c "canMutateRow={() => canMutate}" frontend/src/app/api-keys/page.tsx` returns 1
    - `grep -c "canMutateRow={() => true" frontend/src/app/api-keys/page.tsx` returns 0 (old constant-true gone)
    - `test -f frontend/tests/e2e/phase16_viewer_role.spec.ts` exits 0
    - `cd frontend && npx playwright test tests/e2e/phase16_viewer_role.spec.ts --reporter=list` exits 0
    - `cd frontend && npx --no -- tsc --noEmit` exits 0
    - `.planning/ROADMAP.md` SC-5 wording unchanged: `grep -c "cannot access the create or revoke actions" .planning/ROADMAP.md` returns ≥ 1
  </acceptance_criteria>
  <done>UI matches ROADMAP SC-5 verbatim. Viewer sessions cannot see Create or Revoke. Playwright spec passes for both viewer and owner roles. ROADMAP unedited.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→cloud API | Server-side enforcement (`_viewer_creator_filter`) is already in place and verified — viewers cannot access keys they didn't create regardless of UI affordances. This plan only adjusts the *documentation* or *UI surface*, NOT the security boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-10-01 | Elevation of Privilege | viewer-role API key creation | accept | Server-side `_viewer_creator_filter` already blocks cross-tenant/cross-creator access. UI gating (path a) is defense-in-depth, not the primary control. Path b accepts that the UX permits self-create and the security control sits at the server boundary — equivalent posture. |
| T-16-10-02 | Repudiation | Drift between ROADMAP and CONTEXT decisions | mitigate | Path b records an explicit override entry with rationale and date in 16-VERIFICATION.md frontmatter, so future planners do not re-litigate the contradiction. Path a removes the contradiction by editing UI instead. |
</threat_model>

<verification>
- Exactly one of Task 2 or Task 3 was executed (not both, not neither).
- After execution, `grep "cannot access the create or revoke actions" .planning/ROADMAP.md` and `grep "canMutateRow={() => true" frontend/src/app/api-keys/page.tsx` are NOT both non-empty.
- `cd frontend && npx --no -- tsc --noEmit` exits 0 regardless of which path was taken.
- `/opt/homebrew/bin/pytest tests/test_phase16_*.py -q` exits 0 (server tests untouched by either path).
</verification>

<success_criteria>
- The two-artefact contradiction is resolved
- A future planner reading ROADMAP + CONTEXT for Phase 16 sees a single coherent contract for APIKEY-05
- No regression in the server-side scoping (no test changed for that)
- The chosen path is documented in 16-10-SUMMARY.md
</success_criteria>

<output>
After completion, create `.planning/phases/16-api-key-management/16-10-SUMMARY.md` with:
- Which path was selected (a or b) and the human's stated rationale
- The exact text edit(s) made
- The exact code edit(s) made (path a only) and Playwright run output
- A one-line note: "WR-04 (EditKeyLabelInline saves untrimmed value) is the only remaining Warning from 16-VERIFICATION.md not addressed by 16-07/08/09/10 and is deferred to v1.4 — not in scope here."
</output>
