---
phase: 16-api-key-management
plan: 06
type: execute
wave: 2
depends_on: [16-02]
files_modified:
  - frontend/src/components/BillingStatusBanner.tsx
autonomous: true
requirements: [AUTH-08]
must_haves:
  truths:
    - "BillingStatusBanner.tsx no longer reads localStorage.burnlens_owner_email or session?.ownerEmail in the resend handler (D-13: BillingStatusBanner stops sending a body and stops touching localStorage.burnlens_owner_email; useAuth.ts and setup/page.tsx continue to track owner_email locally for display purposes, but the resend call no longer depends on it)"
    - "POST /auth/resend-verification is called with no body and no Content-Type header (only credentials: 'include')"
    - "Visual UX is unchanged — button copy ('Resend verification email'), in-flight state, toast/messages preserved verbatim from Phase 11"
    - "Build still type-checks (no orphaned references to ownerEmail in this file)"
  artifacts:
    - path: "frontend/src/components/BillingStatusBanner.tsx"
      provides: "Resend handler that depends only on the session cookie (HttpOnly)"
      contains: "credentials: \"include\""
  key_links:
    - from: "frontend/src/components/BillingStatusBanner.tsx::handleResend"
      to: "POST /auth/resend-verification (rewritten in 16-02)"
      via: "fetch with credentials:'include' and no body"
      pattern: "/auth/resend-verification"
---

<objective>
Surgical frontend edit that closes AUTH-08 from the client side. 16-02 made the backend identity-by-JWT; this plan strips the body/localStorage read so a user with `null` `ownerEmail` can successfully trigger the resend.

Purpose: Tiny, isolated change. Wave 2 because it depends on the 16-02 backend contract being live (otherwise the empty body is rejected as 422 by the old endpoint).

Output: One file edit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-api-key-management/16-CONTEXT.md
@.planning/phases/16-api-key-management/16-PATTERNS.md
@.planning/phases/16-api-key-management/16-02-SUMMARY.md
@frontend/src/components/BillingStatusBanner.tsx
@frontend/src/lib/hooks/useAuth.ts
@frontend/AGENTS.md
</context>

<interfaces>
<!-- Existing handler in BillingStatusBanner.tsx lines 32–46 (verbatim from PATTERNS.md): -->

```typescript
async function handleResend() {
  if (resendStatus !== "idle") return;
  setResendStatus("sending");
  try {
    await fetch(`${API_BASE}/auth/resend-verification`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ email: session?.ownerEmail ?? "" }),
      credentials: "include",
    });
    setResendStatus("sent");
  } catch {
    setResendStatus("error");
  }
}
```

<!-- Backend contract post-16-02 (from 16-02 PATTERNS.md):
  POST /auth/resend-verification
    - No request body
    - Requires session JWT cookie (burnlens_session, HttpOnly)
    - Always returns 200 (D-14 enumeration-safe)
-->
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Strip body and localStorage email read from BillingStatusBanner.tsx resend handler</name>
  <read_first>
    - frontend/src/components/BillingStatusBanner.tsx (full file — locate handleResend ~lines 32–46, plus any other reads of `session?.ownerEmail` or `localStorage.burnlens_owner_email`)
    - .planning/phases/16-api-key-management/16-CONTEXT.md (D-12, D-13, D-14)
    - .planning/phases/16-api-key-management/16-PATTERNS.md §"frontend/src/components/BillingStatusBanner.tsx"
    - frontend/AGENTS.md (Next.js note)
  </read_first>
  <files>frontend/src/components/BillingStatusBanner.tsx</files>
  <behavior>
    - The fetch call to `/auth/resend-verification` has only `method: "POST"` and `credentials: "include"`. No `headers`, no `body`.
    - `session?.ownerEmail` and `localStorage.burnlens_owner_email` are not referenced anywhere in this file (grep returns 0 matches).
    - `useAuth.ts` is NOT modified (per CONTEXT D-13: ownerEmail stays in AuthSession for display; only this file's resend call no longer depends on it).
    - All other behavior of BillingStatusBanner — banner visibility logic, button label, in-flight state, toast/text on success/error — stays byte-for-byte identical.
  </behavior>
  <action>
1. Read `frontend/src/components/BillingStatusBanner.tsx` in full. Locate `handleResend` (around lines 32–46 per PATTERNS.md).

2. Replace the existing fetch call with:

```typescript
async function handleResend() {
  if (resendStatus !== "idle") return;
  setResendStatus("sending");
  try {
    await fetch(`${API_BASE}/auth/resend-verification`, {
      method: "POST",
      credentials: "include",
    });
    setResendStatus("sent");
  } catch {
    setResendStatus("error");
  }
}
```

Specifically:
- DELETE the line `headers: { "Content-Type": "application/json" },`.
- DELETE the line `body: JSON.stringify({ email: session?.ownerEmail ?? "" }),`.
- KEEP `credentials: "include"` — this is critical because the server-side `verify_token` reads the `burnlens_session` HttpOnly cookie.

3. **Search for orphaned `ownerEmail` references in this file only:**

```bash
grep -nE "ownerEmail|burnlens_owner_email" frontend/src/components/BillingStatusBanner.tsx
```

If any remaining reference exists in this file (e.g., a banner subtitle that displays the email), evaluate it:
- If purely decorative ("Verify {email} to continue" copy) — leave it as-is. Phase 11 still tracks ownerEmail in localStorage for these display cases per CONTEXT D-13.
- If it's another fetch that should also be JWT-authenticated — STOP and surface the question (this plan only owns the resend handler).

4. **Do NOT modify `frontend/src/lib/hooks/useAuth.ts`** — per CONTEXT D-13, `ownerEmail` stays in the `AuthSession` interface and is still populated from localStorage. Only the resend call's data dependency on it is severed.

5. **Do NOT modify `frontend/src/app/setup/page.tsx`** — it still writes `localStorage.setItem("burnlens_owner_email", ...)` for display purposes (per CONTEXT D-13 / PATTERNS.md "light edit — no change required").
  </action>
  <verify>
    <automated>cd /Users/bhushan/Documents/Projects/burnlens && grep -nE "/auth/resend-verification" frontend/src/components/BillingStatusBanner.tsx | wc -l | grep -q '^1$' && ! grep -nE "ownerEmail|burnlens_owner_email" frontend/src/components/BillingStatusBanner.tsx && ! grep -nE 'body: JSON\\.stringify\\(\\{ email' frontend/src/components/BillingStatusBanner.tsx && grep -nE 'credentials: "include"' frontend/src/components/BillingStatusBanner.tsx | wc -l | awk '{ if ($1 < 1) { print "FAIL: credentials:include must be present"; exit 1 } else print "OK" }' && cd frontend && npx --no -- tsc --noEmit -p tsconfig.json 2>&1 | tail -10 | (! grep -E "error TS")</automated>
  </verify>
  <done>The resend fetch has no body and no Content-Type header but keeps `credentials: "include"`. `grep` confirms zero remaining references to `ownerEmail` or `burnlens_owner_email` in this file. tsc --noEmit passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→server (resend-verification) | Identity now flows exclusively via the HttpOnly `burnlens_session` cookie; the request body is empty and unforgeable from the client. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-06-01 | Spoofing | client-controlled email in body | mitigate | Pre-fix, the client sent `email: session?.ownerEmail` which the server then trusted to look up the user. Post-fix, the body is empty — the server identifies the user from the JWT it signed itself. Eliminates the email-injection surface entirely. Backend mitigation lives in 16-02. |
| T-16-06-02 | Information Disclosure | localStorage owner_email read removed from this path | mitigate | Even though `localStorage.burnlens_owner_email` remains in use elsewhere for display (per D-13), this file no longer participates — removing one read point reduces XSS-leverage exposure for this code path. |
| T-16-06-03 | Cross-Site Request Forgery | empty-body POST is easier to forge | accept | The endpoint is rate-limited (Phase 11 inherited) and always-200 (D-14). Even a forged resend at most triggers an email to the legitimately-authenticated user — equivalent to spamming "I forgot my password" against your own account. SameSite=Lax cookie attribute (Phase 11) further limits this. |
</threat_model>

<verification>
- `grep -nE "/auth/resend-verification" frontend/src/components/BillingStatusBanner.tsx | wc -l` returns 1.
- `grep -nE "ownerEmail|burnlens_owner_email" frontend/src/components/BillingStatusBanner.tsx | wc -l` returns 0.
- `cd frontend && npx --no -- tsc --noEmit` exits 0.
- Manual smoke: log in as a user with `localStorage.burnlens_owner_email` deleted (simulates API-key signup). Click "Resend verification email" → request inspector shows POST with no body → response 200 → toast "If that email is on file…".
</verification>

<success_criteria>
1. AUTH-08 closed end-to-end: paired with 16-02, an API-key-signed-up user can trigger resend without `ownerEmail` in localStorage.
2. Visual UX is unchanged — UI-SPEC §"AUTH-08 — BillingStatusBanner.tsx" specifies "Visual change for AUTH-08: zero."
3. tsc / build still pass.
4. No new orphan code in this file (no unused imports, no dead-code references to ownerEmail in handleResend's scope).
</success_criteria>

<output>
Create `.planning/phases/16-api-key-management/16-06-SUMMARY.md` capturing:
- The before/after fetch call snippet.
- Confirmation that AUTH-08 is closed (paired with 16-02).
- Note: `useAuth.ts` and `setup/page.tsx` were intentionally NOT modified per D-13.
</output>
