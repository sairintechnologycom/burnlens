# burnlens-core

Shared business logic for BurnLens — cost calculation, waste detection, pricing, and provider interfaces.

This package is used by both `burnlens` (local proxy) and the cloud backend.
