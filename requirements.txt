fastapi==0.141.1
# Pinned explicitly (fastapi only floors it at >=0.46.0) because starlette is
# the package the CVEs were in: 0.38.6 carried 9 advisories, the last of which
# (PYSEC-2026-249) is fixed in 1.3.1. An explicit pin keeps pip-audit and
# Dependabot pointed at it and stops a loose fastapi floor resolving backwards.
starlette==1.6.0
uvicorn==0.52.4
asyncpg==0.31.0
pydantic==2.13.5
pydantic-settings==2.15.0
PyJWT[crypto]==2.13.0
bcrypt==5.0.0
httpx==0.28.1
# Pulled in because dashboard_api reuses the OSS recommender (burnlens.analysis)
# whose package __init__ imports the sqlite storage layer.
aiosqlite==0.22.1
python-dotenv==1.2.3
cryptography==50.0.1
python-dateutil==2.9.0
requests==2.34.2
clickhouse-connect==0.7.19
python-multipart==0.0.32
# TOTP second factor. pyotp implements RFC 6238 (and does the HMAC comparison
# with compare_digest); qrcode renders the otpauth:// URI as inline SVG so
# enrollment needs no image host and no frontend QR dependency. Both are pure
# Python with no transitive deps.
pyotp==2.10.0
qrcode==8.2
